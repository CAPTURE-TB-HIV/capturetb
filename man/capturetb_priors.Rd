% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod.R
\name{capturetb_priors}
\alias{capturetb_priors}
\title{Create Prior Distributions for the CaptureTB Model}
\usage{
capturetb_priors(
  mu_alpha.mean = 0,
  mu_alpha.precision = 0.01,
  sigma.rate = 1,
  sigma_alpha.rate = 1,
  beta.mean = c(0, -0.0142, -0.0412, 0.348, 0.352, -0.29),
  beta.precision = c(0.01, 22.7, 13.9, 8.08, 5.5, 23.45)
)
}
\arguments{
\item{mu_alpha.mean}{Numeric scalar. Mean of the prior for \eqn{\mu_\alpha}. Default is 0.}

\item{mu_alpha.precision}{Numeric scalar. Precision (inverse variance) of the prior for \eqn{\mu_\alpha}. Default is 0.01.}

\item{sigma.rate}{Numeric scalar. Rate parameter for the prior on \eqn{\sigma}. Default is 10.}

\item{sigma_alpha.rate}{Numeric scalar. Rate parameter for the prior on \eqn{\sigma_\alpha}. Default is 10.}

\item{beta.mean}{Numeric vector. Means of the priors for the \eqn{\beta} coefficients. Default is \code{c(0, -0.0142, -0.0412, 0.348, 0.352, -0.29)}.}

\item{beta.precision}{Numeric vector. Precision of the priors for the \eqn{\beta} coefficients. Default is \code{c(0.01, 22.7, 13.9, 8.08, 5.5, 23.45)}.}
}
\value{
A list of prior parameters with class \code{"capturetbpriors"}.
}
\description{
Constructs a list of prior distribution parameters for use in the CaptureTB model.
This function validates the input arguments and returns a list with class \code{"capturetbpriors"}.
}
\examples{
priors <- capturetb_priors()

}
